# Fixtures del laboratorio

Los agentes usan `fixture_id` y etiquetas de reglas, no contenido de usuario real.
Las variantes futuras deben ser datos sintéticos, versionados y revisados antes de
entrar al ejecutor. Nunca se deben cargar secretos desde variables de entorno, disco
del usuario o servicios externos.

